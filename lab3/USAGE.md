# 📚 Генератор трехадресного кода (TAC)

**Лабораторная работа 3** — Полнофункциональный компилятор с поддержкой генерации трехадресного кода, семантического анализа и оптимизации кода.

---

## ⭐ Особенности проекта

### ✨ Функциональность

- **Трехадресный код (TAC)** — 15 баллов
  - Преобразование выражений в промежуточный код
  - Управление метками для циклов и условных операторов
  - Поддержка временных переменных
  
- **Семантический анализ** — +3 балла
  - Проверка типов (type checking)
  - Таблица символов
  - Обнаружение ошибок (undefined variables, type mismatches)
  - Подробные сообщения об ошибках и предупреждениях

- **Оптимизация кода** — +4 балла
  - **Constant Folding**: вычисление констант на этапе компиляции
  - **Dead Code Elimination**: удаление неиспользуемых инструкций

- **Встроенный интерпретатор**
  - Виртуальная машина для исполнения TAC
  - Проверка корректности генерируемого кода
  - Поддержка вывода результатов

---

## 🚀 Быстрый старт

### Требования
- C++17 компилятор (g++, clang++)
- GNU Make
- Linux/Unix окружение

### Компиляция

```bash
# Очистить старые файлы
make clean

# Компилировать проект
make

# Компилировать с отладочной информацией
make debug
```

### Запуск

```bash
# Запустить на примере
./bin/compiler test/example1.txt

# Запустить со всеми опциями
./bin/compiler test/example2.txt -tokens -ast -o output/result.tac

# Запустить все тесты
make test
```

---

## 📖 Примеры использования

### Пример 1: Простые вычисления

**Исходный код** (`test/example1.txt`):
```java
int a = 10;
int b = 20;
int c = a + b;
print(c);
```

**Генерируемый TAC**:
```
0:  t1 = a * b
1:  c = t1
2:  print(c)
```

**Результат**: `30`

---

### Пример 2: Циклы и условные операторы

**Исходный код** (`test/example2.txt`):
```java
int sum = 0;
int i = 1;

while (i <= 5) {
    sum = sum + i;
    i = i + 1;
}

if (sum > 10) {
    print(sum);
} else {
    print(0);
}
```

**Генерируемый TAC**:
```
0:  L0:
1:  t1 = i <= 5
2:  ifz t1 goto L1
3:  t2 = sum + i
4:  sum = t2
5:  t3 = i + 1
6:  i = t3
7:  goto L0
8:  L1:
9:  t4 = sum > 10
10: ifz t4 goto L2
11: print(sum)
12: goto L3
13: L2:
14: print(0)
15: L3:
```

**Результат**: `15` (сумма 1+2+3+4+5)

---

### Пример 3: Оптимизация с constant folding

**Исходный код** (`test/example3.txt`):
```java
int result = 0;

for (int i = 0; i < 3; i = i + 1) {
    result = result + (2 * 3);
}

print(result);
```

**TAC до оптимизации**:
```
0:  t1 = 2 * 3
1:  result = result + t1
...
```

**TAC после оптимизации**:
```
0:  t1 = 6               // constant folding: 2 * 3 = 6
1:  result = result + t1
...
```

**Результат**: `18` (6 * 3 итерации)

---

### Пример 4: Факториал (сложная программа)

**Исходный код** (`test/example4.txt`):
```java
int factorial = 1;
int n = 5;
int counter = 1;

while (counter <= n) {
    factorial = factorial * counter;
    counter = counter + 1;
}

print(factorial);
```

**Результат**: `120` (5! = 120)

---

## 🔧 Параметры командной строки

```
./bin/compiler <source_file> [options]

Параметры:
  -tokens            Вывести все токены
  -ast               Вывести AST (Abstract Syntax Tree)
  -noopt             Отключить оптимизацию
  -o <file>          Сохранить TAC в файл
```

### Примеры использования опций

```bash
# Просмотреть все токены
./bin/compiler test/example1.txt -tokens

# Просмотреть AST
./bin/compiler test/example1.txt -ast

# Отключить оптимизацию
./bin/compiler test/example1.txt -noopt

# Сохранить результат в файл
./bin/compiler test/example1.txt -o output/result.tac

# Комбинировать опции
./bin/compiler test/example2.txt -tokens -noopt -o output/debug.tac
```

---

## 📁 Структура проекта

```
.
├── Makefile                    # Правила сборки
├── README.md                  # Этот файл
├── ARCHITECTURE.md            # Описание архитектуры
├── src/
│   ├── lexer.h               # Лексический анализатор
│   ├── parser.h              # Синтаксический анализатор
│   ├── semantic.h            # Семантический анализ
│   ├── ir.h                  # Промежуточное представление
│   ├── codegen.h             # Генератор кода
│   ├── optimizer.h           # Оптимизатор
│   ├── interpreter.h         # Интерпретатор
│   └── main.cpp              # Главная программа
├── test/
│   ├── example1.txt          # Простые вычисления
│   ├── example2.txt          # Циклы и условия
│   ├── example3.txt          # Оптимизация
│   ├── example4.txt          # Факториал
│   ├── error1.txt            # Ошибка типа
│   └── error2.txt            # Неопределённая переменная
├── bin/                       # Скомпилированные файлы
└── output/                    # Выходные файлы TAC
```

---

## 🧪 Тестирование

### Запуск всех тестов

```bash
make test
```

Выполнит:
- 4 позитивных теста (корректные программы)
- 2 негативных теста (обработка ошибок)

### Добавление собственного теста

1. Создайте файл `test/mytest.txt` с исходным кодом
2. Запустите компилятор:
```bash
./bin/compiler test/mytest.txt
```

---

## 💡 Поддерживаемый язык

### Типы данных
```
int      - целое число (32-bit)
bool     - логическое значение (true/false)
```

### Операторы

**Арифметические:**
- `+` сложение
- `-` вычитание
- `*` умножение
- `/` деление
- `%` остаток от деления

**Логические:**
- `&&` логическое И
- `||` логическое ИЛИ
- `!` логическое НЕ

**Сравнения:**
- `==` равно
- `!=` не равно
- `<` меньше
- `>` больше
- `<=` меньше или равно
- `>=` больше или равно

### Конструкции управления потоком

**Условный оператор:**
```java
if (condition) {
    // then branch
} else {
    // else branch
}
```

**Цикл while:**
```java
while (condition) {
    // body
}
```

**Цикл for:**
```java
for (int i = 0; i < 10; i = i + 1) {
    // body
}
```

**Функции:**
```java
print(value);    // встроенная функция вывода
```

---

## 📊 Формат выходного TAC

Каждая инструкция представлена в формате:

```
LINE_NUM:  INSTRUCTION
```

### Типы инструкций

| Тип | Пример | Описание |
|-----|--------|---------|
| Бинарная операция | `t1 = a + b` | Вычисление |
| Унарная операция | `t2 = -a` | Отрицание |
| Присваивание | `x = t1` | Присваивание значения |
| Метка | `L0:` | Точка входа для переходов |
| Переход | `goto L0` | Безусловный переход |
| Условный переход | `ifz t1 goto L1` | Переход если false |
| Печать | `print(x)` | Вывод значения |
| Возврат | `return x` | Возврат значения |

---

## 🎓 Бонусные баллы

### Реализованные бонусы

✅ **Семантический анализ** (+3 балла)
- Проверка типов (type checking)
- Таблица символов
- Обнаружение неопределённых переменных
- Проверка инициализации

✅ **Оптимизация кода** (+4 балла)
- Constant folding (вычисление констант)
- Dead code elimination (удаление мёртвого кода)

### Возможные дополнительные бонусы

🔮 **Перспективы развития** (не реализовано)
- Регистровое выделение памяти (register allocation) — +2 балла
- Генерация нескольких целевых платформ (x86, ARM) — +2 балла
- Интеграция с отладчиком — +2 балла

---

## ⚠️ Обработка ошибок

### Типы ошибок

**ОШИБКИ** (приводят к отказу компиляции):
- Undefined variable: `print(x);` (x не определена)
- Syntax error: нарушение синтаксиса
- Division by zero: деление на ноль

**ПРЕДУПРЕЖДЕНИЯ** (не приводят к отказу):
- Type mismatch: `int a = true;`
- Uninitialized variable: использование переменной до инициализации

### Примеры обработки ошибок

```bash
# Ошибка: неопределённая переменная
./bin/compiler test/error2.txt
# Output: ✗ Undefined variable 'b'

# Предупреждение: несовместимость типов
./bin/compiler test/error1.txt
# Output: ⚠ Type mismatch in assignment to 'a': expected int, got bool
```

---

## 🏗️ Архитектура компилятора

Компилятор состоит из **6 независимых этапов**:

```
Исходный код
     ↓
[1] Лексический анализ      → Токены
     ↓
[2] Синтаксический анализ   → AST
     ↓
[3] Семантический анализ    → Валидированный AST
     ↓
[4] Генерация кода          → TAC (Intermediate Representation)
     ↓
[5] Оптимизация             → Оптимизированный TAC
     ↓
[6] Интерпретация           → Результаты выполнения
```

Для подробного описания архитектуры смотрите файл [`ARCHITECTURE.md`](ARCHITECTURE.md).

---

## 🛠️ Развитие проекта

### Текущий статус

| Компонент | Статус | Баллы |
|-----------|--------|-------|
| Lexer | ✅ Полностью | - |
| Parser | ✅ Полностью | - |
| Semantic Analysis | ✅ Полностью | +3 |
| Code Generator (TAC) | ✅ Полностью | 15 |
| Optimizer | ✅ Полностью | +4 |
| Interpreter | ✅ Полностью | - |
| Documentation | ✅ Полностью | - |
| **ИТОГО** | **✅** | **22** |

---

## 📝 Лицензия и авторство

- **Курс**: Теория языков программирования и методы трансляции
- **Лабораторная работа**: №3 - Генератор кода
- **Язык реализации**: C++17
- **Платформа**: Linux/Unix
- **Автор**: Студент ВолГУ, кафедра ВТ

---

## 🤝 Контакты и поддержка

Для вопросов о проекте:
- Смотрите файл [`ARCHITECTURE.md`](ARCHITECTURE.md) для подробного описания
- Используйте параметр `-tokens` для просмотра этапа лексического анализа
- Используйте параметр `-ast` для просмотра синтаксического дерева

---

**Последнее обновление**: 2025  
**Версия**: 1.0 (Финальная версия для сдачи)
