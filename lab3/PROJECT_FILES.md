# Полный список файлов проекта

## 📄 Документация (4 файла)
- **README.md** - Главный файл с описанием проекта и быстрым стартом
- **USAGE.md** - Подробное руководство использования с примерами
- **ARCHITECTURE.md** - Описание архитектуры компилятора (6 этапов)
- **COMPILATION.md** - Информация о сборке и требованиях

## 🔧 Конфигурация (2 файла)
- **Makefile** - Правила сборки проекта с целями: all, debug, test, clean
- **quickstart.sh** - Bash скрипт для быстрого запуска всех тестов

## 💻 Исходный код (8 файлов)

### src/main.cpp
Главная программа компилятора. Интегрирует все компоненты в единый конвейер обработки:
- Обработка параметров командной строки
- Чтение исходного файла
- Последовательный запуск всех 6 этапов
- Красивый вывод результатов в консоль

### src/lexer.h (~730 строк)
Лексический анализатор (Lexical Analyzer / Tokenizer):
- Класс Token с информацией о позиции в файле
- Класс Lexer для преобразования исходного кода в токены
- Поддержка ключевых слов, операторов, литералов
- Обработка комментариев (// и /* */)
- Автоматическое отслеживание номера строки и столбца

### src/parser.h (~550 строк)
Синтаксический анализатор (Syntax Analyzer / Parser):
- Abstract Syntax Tree (AST) структуры для всех конструкций
- Рекурсивный спуск парсер (Recursive Descent Parser)
- Правильная обработка приоритета операторов
- Поддержка: декларации, присваивания, циклы, условия, вызовы функций
- Обработка ошибок синтаксиса

### src/semantic.h (~300 строк)
Семантический анализатор (Semantic Analyzer):
- Класс Symbol для хранения информации о переменных
- Таблица символов (Symbol Table)
- Проверка типов (Type Checking)
- Обнаружение ошибок:
  - Undefined variable (неопределённая переменная)
  - Variable already defined (повторное определение)
- Предупреждения:
  - Type mismatch (несовместимость типов)
  - Uninitialized variable (неинициализированная переменная)

### src/ir.h (~250 строк)
Промежуточное представление (Intermediate Representation):
- Структуры для операндов (Operand) и инструкций (Instruction)
- Перечисления для всех типов операций (BinOp, UnOp)
- Перечисление типов инструкций (InstrType)
- Структура IRProgram для хранения программы в TAC
- Методы сохранения TAC в файл

### src/codegen.h (~280 строк)
Генератор кода (Code Generator):
- Класс CodeGenerator для преобразования AST в TAC
- Управление счётчиками для временных переменных и метак
- Генерация инструкций для всех типов выражений и операторов
- Поддержка циклов (while, for) с метками и переходами
- Обработка условных операторов (if-else)

### src/optimizer.h (~180 строк)
Оптимизатор кода (Code Optimizer):
- Класс Optimizer для оптимизации программ в TAC
- Constant Folding - вычисление константных выражений на этапе компиляции
  - Примеры: 2 * 3 → 6, 5 > 3 → 1
- Dead Code Elimination - удаление неиспользуемого кода
  - Удаление присваиваний к переменным, которые никогда не используются

### src/interpreter.h (~240 строк)
Интерпретатор (TAC Interpreter / Virtual Machine):
- Класс Interpreter для исполнения программ в TAC
- Таблица переменных для хранения значений
- Счётчик программы (Program Counter)
- Поддержка всех типов инструкций
- Обработка меток и переходов
- Обработка ошибок во время выполнения (деление на ноль и т.д.)

## 🧪 Тестовые файлы (6 файлов)

### test/example1.txt
**Простые вычисления**
```
int a = 10;
int b = 20;
int c = a + b;
print(c);
```
Ожидаемый результат: 30

### test/example2.txt
**Циклы и условные операторы**
```
int sum = 0;
int i = 1;
while (i <= 5) {
    sum = sum + i;
    i = i + 1;
}
if (sum > 10) {
    print(sum);
}
```
Ожидаемый результат: 15

### test/example3.txt
**Демонстрация оптимизации**
```
int result = 0;
for (int i = 0; i < 3; i = i + 1) {
    result = result + (2 * 3);
}
print(result);
```
Показывает constant folding: 2 * 3 → 6

### test/example4.txt
**Сложная программа: факториал**
```
int factorial = 1;
int n = 5;
int counter = 1;
while (counter <= n) {
    factorial = factorial * counter;
    counter = counter + 1;
}
print(factorial);
```
Ожидаемый результат: 120 (5! = 120)

### test/error1.txt
**Негативный тест: ошибка типа**
```
int a = 10;
a = true;    // Type mismatch: assigning bool to int
print(a);
```
Должно вывести предупреждение о несовместимости типов

### test/error2.txt
**Негативный тест: неопределённая переменная**
```
int a = 10;
print(b);    // Error: variable 'b' is not defined
```
Должно вывести ошибку о неопределённой переменной

## 📂 Директории

### bin/
Скомпилированные файлы:
- compiler - Исполняемый файл компилятора

### output/
Выходные файлы:
- Сохраняемый TAC код при использовании опции -o

## 📊 Статистика

### Строки кода
- Исходный код (C++17): ~2500 строк
- Документация (Markdown): ~800 строк
- Тесты: 6 примеров
- Всего: ~3300 строк

### Компоненты
- Header файлы: 7 (ir.h, lexer.h, parser.h, semantic.h, codegen.h, optimizer.h, interpreter.h)
- Главный файл: 1 (main.cpp)
- Конфигурация: 2 (Makefile, quickstart.sh)
- Документация: 4 файла
- Тесты: 6 файлов

## 🎯 Реализованные требования

✅ **Обязательные** (15 баллов):
- Интеграция с лексическим и синтаксическим анализаторами
- Генерация трехадресного кода (TAC)
- Обработка базовых конструкций (присваивание, циклы, условия)
- Вывод генерируемого кода в файл с аннотациями

✅ **Бонусные**:
- Семантический анализ (+3 балла)
- Базовая оптимизация (+4 балла)
- Встроенный интерпретатор

✅ **Дополнительно**:
- Полная документация
- Красивое форматирование вывода
- Примеры использования
- Скрипт быстрого старта

## 🚀 Как использовать проект

```bash
# 1. Компиляция
make clean && make

# 2. Запуск на примере
./bin/compiler test/example1.txt

# 3. Запуск всех тестов
make test

# 4. Быстрый старт со всеми демонстрациями
chmod +x quickstart.sh && ./quickstart.sh

# 5. Дополнительные опции
./bin/compiler test/example1.txt -tokens           # Показать токены
./bin/compiler test/example1.txt -noopt            # Без оптимизации
./bin/compiler test/example1.txt -o output/tac.ir  # Сохранить в файл
```

---

**Версия**: 1.0  
**Дата**: 2025  
**Язык**: C++17  
**Платформа**: Linux/Unix
